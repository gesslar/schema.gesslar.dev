{
  "name": "schema.gesslar.dev",
  "version": "1.0.0",
  "private": false,
  "license": "Unlicense",
  "type": "module",
  "scripts": {
    "generate-docs": "node generate-schema-docs.cjs",
    "watch-schemas": "node watch-schemas.js",
    "prestart": "npm run generate-docs",
    "start": "astro dev",
    "prebuild": "npm run generate-docs",
    "build": "astro build",
    "preview": "astro preview",
    "update": "npx npm-check-updates -u && npm install"
  },
  "dependencies": {
    "@astrojs/starlight": "~0.32.6",
    "astro": "^5.7.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@gesslar/uglier": "^2.2.0",
    "eslint": "^10.0.3"
  },
  "overrides": {
    "@astrojs/sitemap": "3.2.1"
  },
  "engines": {
    "node": ">=20.0"
  }
}
